// constants/session.ts

export const SESSION_CONFIG = {
  // 会话有效期（毫秒）: 12小时
  SESSION_EXPIRES_IN_MS: 12 * 60 * 60 * 1000,
};

export const SESSION_ERRORS = {
  SESSION_NOT_FOUND: 'Session not found',
  SESSION_INVALID: 'Session invalid',
};